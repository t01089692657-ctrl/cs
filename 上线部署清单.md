# 上线部署清单（给 10 人使用）

本文说明：系统部署到服务器后，**数据存哪里、后台如何监管、需要接哪些 API、你需要准备什么**。

---

## 一、数据存放位置

预览版数据存在每个人的浏览器（localStorage），互相独立看不到；
部署后全部迁移到**服务器**，浏览器只是操作界面，10 人共享同一份数据。

| 数据类别 | 存放位置 | 说明 |
|---|---|---|
| 客户 / 潜客 / 线索 / 聊天记录 | 服务器数据库（PostgreSQL 或 MySQL） | 10 人共享，实时同步 |
| 产品资料 / 视频模板 / ICP / 触达话术 | 服务器数据库 | 同上 |
| 产品图片 / 生成的视频文件 | 服务器磁盘 或 对象存储（阿里云 OSS / 腾讯云 COS） | 视频较大，建议对象存储 |
| 邮箱授权码 / 各家 API Key | 服务器端加密存储 | **绝不下发到浏览器** |
| 员工账号 / 权限 / 操作日志 | 服务器数据库 | 审计与追溯 |

**每日自动备份**数据库到对象存储，可恢复到任意一天，防误删。

---

## 二、后台监管（管理后台）

新增「管理员」角色（老板/主管），可：
- 管理 10 个员工账号与**权限分级**（如：底价字段仅老板与销冠可见——系统已预留）
- 查看各员工使用情况：发了多少开发信、跟进了多少客户
- 开发信发送记录审计、AI 操作留痕
- 数据导出、每日自动备份与恢复

---

## 三、需要接入的 API（分三批，逐步接，非一次到位）

### 第一批：核心（接上就能真用）
| API | 用途 | 参考费用 |
|---|---|---|
| 大模型（DeepSeek / 通义千问，任选其一） | AI 打分、写开发信、视频脚本、初筛客服、销冠辅助 | 10 人轻用 ¥50–200/月 |
| 企业邮箱 SMTP（现有邮箱开通即可） | 真实发送开发信 | 免费 |

### 第二批：获客引擎
| API | 用途 | 参考费用 |
|---|---|---|
| Hunter.io / Apollo.io | 按公司域名找负责人邮箱 | $49/月起，有免费额度 |
| SerpAPI / Google Custom Search | 自动搜目标公司 | $50/月起 |
| Firecrawl | 抓取反爬 / JS 重的网页（兜底） | $16/月起 |
| Crawl4AI（自建，随系统部署） | 大批量抓官网 | 免费 |
| Volza / ImportYeti（海关数据） | 真实进口商名单 | ImportYeti 美国免费 |

### 第三批：内容与触达（可延后）
| API | 用途 | 说明 |
|---|---|---|
| 可灵 / 即梦 / 海螺 AI | 真实生成视频 | 按条计费 |
| WhatsApp Business API（360dialog / Twilio） | WhatsApp 真实收发 | 需企业认证 |
| Facebook / Google Ads API | 广告自动投放 | 需开发者审核，周期长 |

**注意事项**
- 微信视频号无公开发布 API：视频生成后仍需人工上传，系统做「待发布」提醒队列。
- Google / Facebook / LinkedIn 等 API 在境内服务器访问受限，**服务器建议选香港或新加坡节点**。

---

## 四、你需要提供的东西

准备好 1–5 项即可上线，第二、三批 API 按需陆续给：

1. **服务器**：阿里云/腾讯云，推荐 4核8G、**香港或新加坡**节点（约 ¥150–300/月）→ 提供 IP + root 密码或 SSH 密钥
2. **域名**（建议）：如 `crm.公司名.com` → 提供域名管理权限（配置解析）
3. **企业邮箱**：邮箱地址 + SMTP 授权码（邮箱设置里生成）
4. **大模型 API Key**：推荐先开 DeepSeek
5. **10 个使用者名单**：姓名 / 邮箱 / 角色（管理员 / 销售 / 运营）→ 建账号与权限
6. 第二、三批 API Key：接一个激活一个功能

---

## 五、上线后的样子

给齐第 1–5 项后，我会完成：服务器环境、数据库、员工登录系统、后端服务（含获客引擎与发信），
并把现有前端接上去。届时 10 个人打开网址登录，界面与当前预览版**完全一致**，
差别只是数据变成公司共享的真实数据、各 AI 功能真实生效。

> 前端所有对外调用都收口在 `cc/js/services.js`；接 API 只改这一处，页面零改动。
> 获客引擎的技术细节见《获客引擎-部署说明.md》。
