# 主动客户开发 = 自动获客系统（联邦接入）

「主动客户开发」已接入一套独立、更强的**自动获客系统**（代号 ZDHK，源码在 `services/getke/`）：
多源找客户 → 邮箱瀑布富化+验证 → 送达率基建（域名池/预热/频控）→ AI 写信/回复分诊 → 轻 CRM。
它是 **Python(FastAPI) + Next.js** 一整套应用，比原来内置的轻量获客模块强一个量级。

## 融合方式：联邦（不是重写）

- 这套系统作为**独立服务**运行（它自带后端 API 与前端工作台）。
- 主系统的「主动客户开发」菜单在**在线模式**下**内嵌**它的工作台（iframe）。
- 没配置时（`GETKE_WEB_URL` 为空），「主动客户开发」仍用主系统自带的轻量获客模块——
  所以演示/预览不受影响。

为什么不重写成 Node：它已有 62 项测试、送达率与合规基建成熟，重写会丢掉这些价值并引入新 bug。
联邦保留它的能力，改动最小。

## 怎么启动（Mac mini）

1. 先按它自己的说明把这套系统跑起来（零外部依赖的本地模式即可）：
   ```bash
   cd services/getke
   bash mac-quickstart.sh        # 装依赖 + 灌演示数据 + 起后端(8000)与前端(3000)
   # 或 Docker：cp .env.example .env && docker compose up --build
   ```
   跑起来后：前端 http://<Mac的IP>:3000 、API 文档 http://<Mac的IP>:8000/docs
2. 在主系统的 `server/.env` 里填：
   ```
   GETKE_WEB_URL=http://<Mac的IP>:3000
   ```
   重启主系统服务。此后员工在主系统点「主动客户开发」，看到的就是这套自动获客工作台。

## 上真实数据

`services/getke/.env` 把 `ADAPTER_MODE=local` 改成 `cloud` 并填真实密钥（大模型/海关/发信…），
业务代码零改动。它自己的文档更详细：
- `services/getke/docs/06-上线接入指南.md`（要接哪些服务、拿哪些 Key、三档配置）
- `services/getke/docs/07-价格总览.md`（各服务价格 + 三档月成本）
- `services/getke/README.md`（架构与快速开始）

## 已知后续项

- **单点登录（SSO）**：目前主系统与获客系统是两套登录。做到"一次登录全站通"需要两边共享会话，
  是下一步的打磨项；当前联邦在导航层打通（一个门户、点进去即用）。
- 也可做成同一域名下的反向代理，让员工只用一个网址访问（进一步减少端口暴露）。
