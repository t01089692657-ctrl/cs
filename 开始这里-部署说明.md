# 开始这里 · Mac 部署说明

这是「外贸全链路 AI 系统」的完整代码包。解压后按下面走。

## 这个包里有什么

| 目录/文件 | 是什么 |
|---|---|
| `cc/` | 前端（10 个业务模块的界面） |
| `server/` | 主后端（登录 / 数据库 / 发信 / 视频真出片 / 管理后台） |
| `services/getke/` | 自动获客系统（Python，更强的获客后端，独立启动） |
| `deploy/` | 一键部署脚本（mac-setup.sh 等） |
| `*.md` | 各类说明文档（见下方"文档导航"） |

> 注意：包里**不含**依赖库和密钥（`node_modules`、Python 虚拟环境、`.env`）。
> 部署脚本会自动装依赖；密钥你按文档填。

## 三步跑起来（主系统）

在 Mac 上打开「终端」，`cd` 到解压出来的这个目录，然后：

```bash
bash deploy/mac-setup.sh
```

它会自动：装 Node + PostgreSQL + ffmpeg → 建库 → 生成配置 → 装依赖 → 设为开机自启并启动。
跑完会打印本机地址和局域网地址（发给同事的那个）。

然后编辑 `server/.env` 设好管理员账号密码、按需填各 API key，重启服务即可（脚本末尾有命令）。

## 想启用"自动获客系统"（可选，更强的获客）

主系统的「主动客户开发」可以接入 `services/getke/` 那套更强的系统：

```bash
cd services/getke
bash mac-quickstart.sh          # 起它的后端(8000)与前端(3000)
```

再在主系统 `server/.env` 填 `GETKE_WEB_URL=http://<你Mac的IP>:3000`，重启主系统即生效。
细节见《联邦-自动获客系统.md》。

## 文档导航

| 文档 | 看它了解 |
|---|---|
| **《Mac-mini-部署指南.md》** | 主系统在 Mac 上的完整部署：保持唤醒/开机自启、局域网 vs 远程访问、备份、境内访问海外 API 的代理配置 |
| 《上线部署清单.md》 | 数据存哪、后台监管、要接哪些 API、你需要准备什么 |
| 《联邦-自动获客系统.md》 | 怎么启用并接入自动获客系统 |
| 《获客引擎-部署说明.md》 | 主系统内置获客引擎的技术细节（若不用 getke） |
| 《版本记录.md》 | 每个版本改了什么，可回退 |
| `services/getke/docs/` | 自动获客系统自己的产品/技术/上线/价格文档 |

## 需要帮忙

装的过程中卡住，把终端里的报错发给我，我帮你看。或者把 Mac 的访问方式给我，我远程帮你部署。
