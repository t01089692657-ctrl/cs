# 部署指南

这套系统现在是**部署即可用**的产品：一套代码两种模式，自动切换。

- **演示模式**：直接用浏览器打开 `cc/index.html`（或看在线预览）。数据存本地，单机体验。
- **在线模式**：部署到服务器后，自动变为多人共享：登录、真实数据库、真实获客与发信。

代码**不用改**——部署时只填一个 `.env` 文件，系统按你填了哪些 key 自动开启对应能力。

---

## 一、最快上线（3 步）

服务器需已安装 Docker 与 docker compose（香港/新加坡节点，4核8G 推荐）。

```bash
# 1) 拉代码
git clone <你的仓库地址> && cd cs

# 2) 配置：复制模板并填 key
cp server/.env.example server/.env
nano server/.env        # 至少填 JWT_SECRET、ADMIN_EMAIL、ADMIN_PASSWORD

# 3) 一键启动（数据库 + 应用 + 抓取引擎）
bash deploy/deploy.sh
```

启动后访问 `http://<服务器IP>:8080`，用 `.env` 里的管理员账号登录，进「管理后台」创建其余 9 个员工账号。

> 生产建议再配 nginx + 域名 + HTTPS（见 `deploy/nginx.conf`，证书用 `certbot` 免费签发）。

---

## 二、`.env` 分批填，能力逐步开启

系统对每项能力都是「填了 key 就真实生效，没填就演示」，所以可以分批来：

**第一批（必填，接上就能真用）**
- `JWT_SECRET`：随机长字符串
- `ADMIN_EMAIL` / `ADMIN_PASSWORD`：首个管理员账号
- `DATABASE_URL`：用 docker compose 时自动注入，无需手填
- `LLM_PROVIDER` + `LLM_API_KEY`：大模型（推荐 deepseek）
- `SMTP_*`：企业邮箱，用于真实发开发信

**第二批（获客引擎）**
- `CRAWL4AI_URL`：compose 已自动注入内网地址
- `FIRECRAWL_API_KEY`：反爬兜底
- `SERPAPI_KEY`：搜索找公司
- `EMAIL_FINDER_PROVIDER` + `EMAIL_FINDER_KEY`：Hunter/Apollo 找邮箱

**第三批（内容与触达，可延后）**
- `VIDEO_API_*`：真实视频生成
- `WHATSAPP_API_*`：WhatsApp 收发

改完 `.env` 后 `docker compose up -d` 重启生效。

---

## 三、这套系统包含什么

| 能力 | 说明 |
|---|---|
| 登录 + 权限 | 管理员/销售/运营三种角色，底价等敏感字段按角色可见 |
| 共享数据库 | 产品/模板/潜客/视频等 10 人共享，实时同步，每日备份 |
| 管理后台 | 员工账号管理 + 全操作审计（谁在何时做了什么） |
| 获客引擎 | 大模型 + Crawl4AI/Firecrawl + 搜索 + 找邮箱，一键全渠道获客 |
| 发信 | 用你的企业邮箱 SMTP 真实群发开发信、自动个性化 |

---

## 四、架构（技术对接参考）

```
浏览器（cc/ 前端，自动探测后端）
   │  同域 /api/*
   ▼
server/（Node + Express）
   ├─ 认证 JWT + 角色
   ├─ 共享数据  → PostgreSQL
   ├─ 获客 agent → LLM + Crawl4AI/Firecrawl + SerpAPI + Hunter/Apollo
   ├─ 发信       → 企业邮箱 SMTP
   └─ 管理后台   → 用户 / 审计
```

- 所有对外调用集中在 `server/src/services/`（llm/crawl/search/emailFinder/mailer/harvest）与前端 `cc/js/services.js`。
- 前端如何判断走后端：启动探测 `GET /api/health`，探测到即在线模式。
- 无 `DATABASE_URL` 时用内存库（演示），便于本地先跑通。

数据库结构见 `server/src/db/schema.sql`；获客引擎细节见《获客引擎-部署说明.md》；
所需服务器/域名/账号见《上线部署清单.md》。
